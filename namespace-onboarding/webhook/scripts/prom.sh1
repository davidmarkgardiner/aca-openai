## always run unless the $resourceGroupName contains at54395
if [[ "$resourceGroupName" != *"at54395"* ]]; then
  az aks update 
else
  if [[ "$resourceGroupName" = *"at54395"* ]]; then
    ## if resource group contains at54395 then run
    az aks update
  fi
fi